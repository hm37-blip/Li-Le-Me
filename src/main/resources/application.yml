server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lilema?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: Gregon473466
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    time-zone: Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

wechat:
  appid: your_appid
  secret: your_secret
  mock-openid: "dev_user_001"   # 本地开发用，设为空字符串则调用真实微信接口

logging:
  level:
    com.cesa.lilema: DEBUG
